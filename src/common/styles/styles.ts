import {StyleSheet} from 'react-native';

export const commonStyles = StyleSheet.create({
    input: {
        height: 40,
        borderRadius: 10,
        padding: 5,
        width: '60%',
        backgroundColor: '#fff'
    },
});